# Apply with `kubectl apply -f secret.example.yaml` after replacing the
# placeholders. Or use a SealedSecret / ExternalSecret in production.
apiVersion: v1
kind: Secret
metadata:
  name: stripe-to-amplitude
  namespace: default
type: Opaque
stringData:
  STRIPE_API_KEY: "rk_live_xxx"
  STRIPE_WEBHOOK_SECRET: "whsec_xxx"
  AMPLITUDE_API_KEY: "your_amplitude_api_key"
